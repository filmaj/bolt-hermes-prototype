{
    "manifest": {
        "command": {
            "default": "npm run manifest --silent"
        }
    },
    "run": {
        "command": {
            "default": "source .env && node index.js"
        },
        "watcher": {
            "filter_regex": "^.*\\.js$",
            "paths": [
                "./"
            ]
        }
    }
}
